## v5.3.7 更新（nomodel 单文件 ~1.71GB）

本版为无模型版：单文件即装，不含模型权重（安装后网页内一键下载卡片 / `下载模型.bat` / 手动放入均可）。

### 音色档案
- 随包 8 个档案（7 克隆 + 甜妹女生无参考预设），7 个档案参考 wav 内置（2.9MB，源码已随 git 入库可溯源）——**装完即可试听克隆音色**
- 音色档案/合成记录**可达性校验**：wav 丢失的记录自动从列表 + 恢复备份中清除并持久化（`/api/history/check`）

### Web UI（使用手册 §12）
- 新增「合成试听」卡（波形 + 播放/下载）
- 共享波形/调速条并入「最近合成记录」标题行；待合成文本框固定 3 行；seed 参数
- 命令行窗口 toggle 常显（有原生控制台→显/隐；无→日志 tail 窗）
- 深/浅主题；设置页新增 qwen3 时间戳模型**单独下载/删除**入口

### 时间戳
- 双引擎 qwen3/whisper 字符/词级时间戳（使用手册 §11）
- qwen3 模型**不随包**，首用自动下载（`hf_hub_download`；无网报错退出、CLI 降级 whisper）

### OpenAI 兼容 API
- `Scripts\voxcpm_openai.py`（:8020 → 后端 WebUI :19001）+ `Scripts\start_openai_api.bat` 一键拉起
- 快速参考：`OpenAI_API_QuickRef.md`（安装目录根，随包；GitHub 仓库内亦可阅读）

### 文档
- `app/VoxCPM2_使用手册.md` v5.3.7（12 章：安装护栏/三种用法/高级参数/响应头护栏/FAQ/官方特性对照/双引擎时间戳/Web UI 更新说明）

### 安装
- 安装路径必须纯 ASCII（kaldi-fst 护栏）；桌面 2 快捷方式（交互菜单 / 网页界面）
- 静默安装：`/SILENT`（默认目录）或 `/SILENT /DIR="D:\VoxCPM2"`
