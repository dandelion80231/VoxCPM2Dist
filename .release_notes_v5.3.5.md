本次版本在 **v5.3.4（多音字修正 LoRA 微调通道）** 基础上，聚焦两处增强：① 修复网页端 LoRA 挂载状态面板不实时刷新的体验问题；② 将多音字 LoRA 训练的完整经验文档与语料工具链正式入库，便于开发者复现与扩展。

> 安装包均为**无模型版**（约 1.87 GB），首次运行需按提示下载主模型权重。本 Release 提供 **完整版** 与 **精简版** 两个选择，详见下方「安装包选择」。

## 安装包选择

本 Release 提供 **两个无模型安装包**，按需下载其一即可（不要重复安装）：

| 安装包 | 文件 | 包含内容 |
| --- | --- | --- |
| **完整版** | `VoxCPM2_TTS_v5.3.5_nomodel_Setup.exe` | 4 项核心修复 + 2 个新验证脚本（`verify_listen.py` / `verify_xing.py`）+ 多音字 LoRA 训练工具链与完整经验文档（`Scripts/training/polyphone_corpus/` 共 36 文件） |
| **精简版** | `VoxCPM2_TTS_v5.3.5_nomodel_lite_Setup.exe` | 仅 4 项核心修复（`vox_web_ui.py` / `lora_helper.py` / `verify_lora.py` / `voxcpm_tts_v5_longtext.py`），不含训练工具链、不含新验证脚本 |

### 选择建议

- **只想用 TTS、不打算训练 LoRA** → 选 **精简版**。脚本少、安装目录更干净，不带训练用文件。
- **想训练多音字 LoRA / 查看完整训练经验 / 使用 `verify_listen.py` 与 `verify_xing.py` 听测工具** → 选 **完整版**。训练经验、语料工具链、听测工具全部开箱即用。
- **不确定** → 选 **精简版**。以后想训练时，可单独从仓库 `main` 分支拉取 `app/Scripts/training/polyphone_corpus/` 目录即可。

> 两者基于同一份核心代码，仅训练 / 验证工具有差异；网页端与 CLI 的 TTS 使用体验完全一致。

## 关键修复

### 网页端 LoRA 挂载状态面板实时刷新
- 修复网页 UI（`Scripts/vox_web_ui.py`）中「多音字 LoRA 权重」状态栏挂载指示不随挂载/卸载实时刷新的问题：挂载或卸载 LoRA 后，状态面板现在会立即回显最新挂载状态与权重路径，无需手动刷新页面。
- 配套 `Scripts/lora_helper.py` 增加状态回读接口，供网页端拉取真实挂载态。

## 新增内容

### 多音字 LoRA 训练工具链与经验文档（`Scripts/training/polyphone_corpus/`）
将此前分散的语料处理/评估脚本与踩坑经验统一归档，共 **36 个文件**，开箱即用：
- `LORA_TRAINING.md` / `README_v23_完整经验.md`：从数据采集、标注、训练到评估的完整经验与参数复盘。
- 语料构建：`append_hao_sentences.py`、`build_v23.py`、`convert_polyphone.py`、`v19_to_pinyin.py`、`v20_build.py`、`merge_v19_v23.py` 等。
- 评估校验：`asr_eval.py`、`verify_asr.py`、`audit_dataset.py`、`diff_web_pinyin.py`、`verify_dict_sources.py`、`test_pypinyin_dict.py` 等。
- 诊断与听感：`diag_listen.py`、`gen_diag_testset.py`、`run_diag_infer.py`、`build_listen_html.py`、`monitor_progress.py`、`synth_batch.py` 等。
- 训练主程序：`train_lora.py`，以及 `data/` 下配套语料与字典。

### 新增验证脚本
- `Scripts/verify_listen.py`：批量生成听感对比样本并产出可浏览的 HTML，便于人工评估多音字改善。
- `Scripts/verify_xing.py`：针对「性/姓」等同音多义场景的专项校验。

### 推理侧（沿用 v5.3.4 能力）
- 网页 UI 与 CLI（`voxcpm_tts_v5_longtext.py`，`--lora` / `VOXCPM_LORA`）的 LoRA 一键挂载保持不变，本次仅增强状态回显。

## 使用方式（训练）
```powershell
cd D:\AI\Build\VoxCPM2Dist\app\Scripts\training\polyphone_corpus
# 详见 LORA_TRAINING.md / README_v23_完整经验.md
python train_lora.py
```

## 完整提交
基于 `main` 分支，本次增量更新涵盖上述状态面板刷新修复与 `polyphone_corpus/` 工具链入库。
